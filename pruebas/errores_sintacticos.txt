main{

	local{
		int	entero1, entero2;
		float real1;
		char carac1, carac2, carac3;
		bool logico1;

		list_of int lista1 ñ;

		char error, de tipo2
		int mierror detipo2;
	}

	 int func1 (int a, float b){
	 	lista1 = lista1 ++ a @ b;
	 	return lista1;
	 }

	int funcWithError(int , int b) {
		return b;
	}

	int funcWithError2( myId, int b) {
		return b;
	}

	int erroresAritmeticos() {
		a b;
		(a / b) * (a - c) - ;
		(a / b) (a - c) - d;

		a = b;

		a / b) * (a - c);

		estoEsCorrecto = a + - b;
		estoTambien = a / b * - c;
	}

	int erroresTipoCinco() {
		if faltan == parentesis {
			detecta + dos + errores;
		}

		a = ;

	 	do{

	 	}until false;

		a = b;
		return ;
	}

	errorFinal = añ;

	return 0;
}
